import Logo from "../img/logo.svg";
const NavBar = () => {
  return (
    <div className="navbar bg-red-700 flex justify-center">
      <img src={Logo} alt="Margel Logo" />
    </div>
  );
};

export default NavBar;
